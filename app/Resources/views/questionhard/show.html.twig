{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="text-center">Questions</h1>

    <div class="col-md-4 col-md-offset-5">

        <a href="{{ path('questionhard_index') }}">Retour à la liste</a>

        <a href="{{ path('questionhard_edit', { 'id': questionHard.id }) }}">Editer</a>
    </div>

    <div class="row">
        <table class="col-md-2 col-md-offset-4">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ questionHard.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ questionHard.title }}</td>
            </tr>
            <tr>
                <th>Answer1</th>
                <td>{{ questionHard.answer1 }}</td>
            </tr>
            <tr>
                <th>Answer2</th>
                <td>{{ questionHard.answer2 }}</td>
            </tr>
            <tr>
                <th>Answer3</th>
                <td>{{ questionHard.answer3 }}</td>
            </tr>
            <tr>
                <th>Answer4</th>
                <td>{{ questionHard.answer4 }}</td>
            </tr>
            <tr>
                <th>Goodanswer</th>
                <td>{{ questionHard.goodanswer }}</td>
            </tr>
        </tbody>
    </table>
    </div>

    <div class="col-md-2 col-md-offset-5">
        {{ form_start(delete_form) }}
        <input type="submit" value="Delete">
        {{ form_end(delete_form) }}
    </div>

{% endblock %}
